import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.ionic.productmanager',
  appName: 'product-manager-clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
